@model IEnumerable<Restaurant.Areas.Admin.ViewModels.TransactionContactUsModel>

@{
    ViewData["Title"] = "Index";
}

<div class="pagetitle">
    <h1>Transaction Contact Us</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
            <li class="breadcrumb-item active"><a asp-action="Index" asp-controller="TransactionContactUs">Transaction Contact Us</a></li>
          
        </ol>
    </nav>
</div>
</hr>
<section>


    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Multi Columns Form</h5>
                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th>
                                    Name
                                </th>
                                <th>
                                    Email
                                </th>
                                <th>
                                    Subject
                                </th>
                                <th>
                                    Message
                                </th>
                                
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionContactUsFullName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionContactUsEmail)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionContactUsSubject)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionContactUsMessage)
                                    </td>

                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
