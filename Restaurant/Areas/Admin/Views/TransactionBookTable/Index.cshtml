@model IEnumerable<Restaurant.Areas.Admin.ViewModels.TransactionBookTableModel>

@{
    ViewData["Title"] = "Index";
}

<div class="pagetitle">
    <h1>Transaction Book Table</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Home</a></li>
            <li class="breadcrumb-item active"><a asp-action="Index" asp-controller="TransactionBookTable">Transaction Book Table</a></li>
           
        </ol>
    </nav>
</div>

<section>


    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Multi Columns Form</h5>
                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th>
                                    Name
                                </th>
                                <th>
                                    Email
                                </th>
                                <th>
                                    MobileNumber
                                </th>
                                <th>
                                    TableDate
                                </th>
                                
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionBookTableFullName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionBookTableEmail)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionBookTableMobileNumber)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TransactionBookTableDate)
                                    </td>

                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
